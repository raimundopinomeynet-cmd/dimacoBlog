### Para correr los estilos, es necesario abrir una consola y situarnos en la carpeta sass con el comando:
```
 cd .\styles\sass\
```

### y despues ejecutar sass con el siguiente comando 
```
sass --watch --no-source-map --style=expanded ../sass:../css
```